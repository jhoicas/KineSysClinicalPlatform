run:
	go run ./cmd/api

build:
	go build -o bin/api ./cmd/api

test:
	go test -v ./...

clean:
	rm -rf bin/

migrate:
	@echo "Remember to run migrations manually against your Supabase DB or use supabase CLI."
