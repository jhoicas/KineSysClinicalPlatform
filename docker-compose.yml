version: "3.8"

services:
  frontend:
    build: ./frontend
    ports: ["3005:3000"]
    environment:
      - VITE_API_BASE_URL=http://backend:8080
    depends_on: [backend]

  backend:
    build: ./backend
    ports: ["8085:8080"]
    env_file: ./backend/.env