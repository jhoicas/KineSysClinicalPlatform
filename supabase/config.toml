# KineSys — configuración Supabase CLI (Edge Functions)

# El preflight OPTIONS del navegador no lleva JWT; si verify_jwt=true
# el gateway puede responder 401 y el browser reporta fallo de CORS.
[functions.invite-professional]
verify_jwt = false

[functions.send-patient-document]
verify_jwt = false
